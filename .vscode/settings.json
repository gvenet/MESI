{
   "sqltools.connections": [
      {
         "previewLimit": 50,
         "driver": "SQLite",
         "database": "${workspaceFolder:MESI}/back/database.db",
         "name": "db-map"
      }
   ]
}